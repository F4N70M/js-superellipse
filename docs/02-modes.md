# Режимы работы

Библиотека предоставляет два режима применения суперэллипса: `clip-path` и `svg-layer`.  
Выбор режима влияет на производительность, поддерживаемые CSS-свойства и сложность DOM-структуры.

## Сравнение режимов

|	Характеристика	|	|	`clip-path`	|	|	`svg-layer`	|
|----------------|:---:|:---:|:---:|:---:|
|	**Способ реализации**	|	|	CSS `clip-path`	|	|	Создание дополнительного SVG слоя и обертки для контента,<br>Перенос css стилей	|
|	**Дополнительные DOM-узлы**	|	|	Нет	|	⚠️	|	Да	|
|	**CSS внешнего контура**	|	❌	|	`border`, `outline`, `box-shadow`, `filter: blur`, `filter: drop-shadow` обрезаются	|	✅	|	Большинство свойств внешнего контура `path` переопределяется в дополнительных DOM-узлах	|
|	**CSS комбинаторы**	|	|	Не влияет	|	⚠️	|	Комбинаторы прямых потомков `element` `>` `child` могут работать некорректно из-за дополнительных DOM-узлов	|
|	**Производительность**	|	|	Высокая<br>(только обрезка)	|	⚠️	|	Ниже при большом количестве элементов с примененным режимом<br>(рендер SVG + фильтры)	|

---

## Поддержка CSS стилей

|	Свойство	|	|	Режим `clip-path`	|	|	Режим `svg-layer`	|
|:---|:---:|:---:|:---:|:---:|
|	`clip-path`	|	|	Переопределяется<br>Запоминает оригинальное значение для возврата при отключении режима	|	|	Сбрасывается<br>Запоминает оригинальное значение для возврата при отключении режима	|
|	`border-radius`	|	✅	|	Сбрасывается;<br>Переопределяется свойством `clip-path` элемента	|	✅	|	Сбрасывается;<br>Перепределяется через `path` svg-слоя	|
|	`background`	|	✅	|	Без изменений	|	✅	|	переносится в `foreignObject` svg-слоя	|
|	`border`	|	❌	|	Не поддерживается<br>(сбрасывается)	|	✅	|	Сбрасывается;<br>Переопределяется свойством `stroke` в svg-слое	|
|	`box-shadow`	|	❌	|	Обрезается свойсвом clip-path элемента	|	✅	|	Сбрасывается;<br>Перепределяется через фильтры svg-слоя	|
|	`filter: blur`, `filter: drop-shadow`	|	❌	|	Обрезается свойсвом clip-path элемента	|	✅	|	Нативно	|
|	`outline`	|	❌	|	Обрезается свойсвом clip-path элемента	|	✅  |	Сбрасывается;<br>Перепределяется через фильтры svg-слоя	|
|	`overflow:hidden`	|	❌	|	Не влияет	|	❌	|	Не поддерживается<br>(сбрасывается)	|
|	`border-image`	|	❌	|	Не поддерживается	|	❌	|	Не поддерживается	|
|	`transition`	|	⚠️	|	Частично поддерживается<br>(кроме border-radius)	|	⚠️	|	Частично поддерживается<br>(не сброшенные свойства)	|
|	`animation`	|	⚠️	|	Частично поддерживается<br>(кроме border-radius)	|	⚠️	|	Частично поддерживается<br>(не сброшенные свойства)	|

> При актуализации CSS свойств временно отключает свойства transition и animation для определения значений остальных свойств из CSSStyleDeclaration элемента

<!-- |	mask	|	—	|	—	|	—	|	—	|	—	| -->
<!-- |	filter	|	—	|	—	|	—	|	—	|	—	| -->
<!-- |	backdrop-filter	|	—	|	—	|	—	|	—	|	—	| -->

---

## Режим `clip-path`

Использует CSS-свойство `clip-path: path("...")` для обрезки элемента по форме суперэллипса.

### Особенности

- Не создаёт дополнительных DOM-узлов.
- Исходный `background`, `border`, `outline`, `box-shadow`, `filter: blur`, `filter: drop-shadow` элемента **обрезаются** по форме суперэллипса (но не переносятся в слой, из-за чего могут выглядеть некорректно).
- Подходит для простых элементов без сложного оформления (например, изображения, иконки).

### Пример

```js
const element = document.querySelector('.image');
const controller = element.superellipseInit({ mode: 'clip-path' });
```

### Когда использовать

- Нужна максимальная производительность.
- Элемент не имеет фона, границ или теней.
- Элемент – это изображение, и его обрезка по форме – единственное требование.
- Элемент находится в анимированном или часто перерисовываемом контексте.

---

## Режим `svg-layer`

Полнофункциональный режим, создающий наложенный SVG-слой, который повторяет геометрию суперэллипса и отрисовывает фон, границы и тени.

### Как это работает

1. Исходный элемент получает `position: relative` и очищается от фона/границ/теней.
2. Создаётся внутренний `div` (`class="jsse--svg-layer--content"`), в который перемещаются все дочерние узлы.
3. Создаётся SVG-элемент под внутренним `div`.
4. SVG содержит:
   - Путь (`<path>`) формы суперэллипса.
   - Фильтры для теней (`box-shadow`).
   - `foreignObject`, куда скопированы вычисленные стили `background` и `box-shadow`.
   - Обводку для границ (`border`).
5. Все стили синхронизируются при изменениях.

### Особенности

- Поддерживает `background`, `border`, `box-shadow` в полном объёме.
- Поддерживает градиенты, сложные фоны (изображения, цвета).
- Требует больше ресурсов из-за рендеринга SVG и фильтров.
- Не влияет на события мыши (у SVG `pointer-events: none`, поэтому клики проходят сквозь).

### Пример

```js
const card = document.querySelector('.card');
const controller = card.superellipseInit({ mode: 'svg-layer', curveFactor: 1.2 });
```

### Когда использовать

- Элемент имеет границы или тени.
- Нужно сохранить оригинальное визуальное оформление.
- Не критична производительность на очень большом количестве элементов.
- Требуется поддержка сложных стилей.

---

## Переключение режима

Вы можете переключить режим на лету у уже инициализированного элемента:

```js
// Переключение с svg-layer на clip-path
controller.switchMode('clip-path');

// Обратно
controller.switchMode('svg-layer');
```

При переключении:
- Старый режим уничтожается (виртуальные элементы удаляются, стили восстанавливаются).
- Новый режим активируется с текущими настройками (`curveFactor`, `precision`).

---

## Рекомендации

|	Сценарий	|	Рекомендуемый режим	|
|----------|---------------------|
|	Изображения, аватары, иконки	|	`clip-path`	|
|	Карточки с фоном и тенями	|	`svg-layer`	|
|	Кнопки с градиентной границей	|	`svg-layer`	|
|	Анимированные элементы (много кадров)	|	`clip-path`	|
|	Сложные фоны (изображения, паттерны)	|	`svg-layer`	|
|	Мобильные устройства (экономия батареи)	|	`clip-path`	|

---

## Известные ограничения режимов

### `clip-path`
- Тени и границы обрезаются, но не отображаются корректно.
- Некоторые браузеры могут отображать `clip-path` с артефактами при скруглении.

### `svg-layer`
- `border-style: double` не поддерживается (выдаётся предупреждение).
- `box-shadow` с `inset` не поддерживается (игнорируется).
- При большом количестве элементов (десятки и сотни) может снижать производительность.
- SVG-слой создаёт дополнительную вложенность, что может влиять на CSS-селекторы типа `:first-child`.

---

## Пример: сравнение визуального результата

### Исходный элемент с фоном, границей и тенью

```css
.card {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border: 2px solid white;
  box-shadow: 0 10px 20px rgba(0,0,0,0.2);
  border-radius: 30px;
}
```

|	Режим	|	Результат	|
|-------|-----------|
|	`clip-path`	|	Фон и тень обрезаются по форме, граница может выглядеть неровно.	|
|	`svg-layer`	|	Фон, граница и тень идеально повторяют форму суперэллипса.	|

Выбирайте режим в зависимости от визуальных требований вашего проекта.

---

## Разделы

- [Введение](./00-index.md) – Основная информация
- [API Reference](./01-api.md) – все методы, классы и опции
- [Режимы работы](./02-modes.md) – `clip-path` vs `svg-layer`
- [Продвинутые возможности](./03-advanced.md) – hover-триггеры, события, отладка
- [Примеры использования](./04-examples.md) – готовые фрагменты кода
- [FAQ и ограничения](./05-faq.md) – известные проблемы и решения